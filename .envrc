#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "ahl_health_apps_analysis")"
[ "$CONDA_DEFAULT_ENV" = "ahl_health_apps_analysis" ] || echo "Have you run \`make install\`?"
